#include <stdio.h>

int Untitled3(int argc, char** argv) {
	/* Imprima o numero de 1 a 100 utilizando for / while / do while*/
	
	int numero=1;
	
	do{
		printf("%d",numero++);
	}while(numero<101);
	return 0;
}
