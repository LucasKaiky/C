#include <stdio.h>

int Untitled2(int argc, char** argv) {
	/* Imprima o numero de 1 a 100 utilizando for / while / do while*/
	
	int numero=1;
	
	while (numero > 0 && numero <101){
		printf("%d",numero++);
	}
	return 0;
}
